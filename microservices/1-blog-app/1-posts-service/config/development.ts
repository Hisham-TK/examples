import { PartialDeep } from '../src/common/types/partialDeep.type';
import { IConfig } from './config.interface';

const configurations: PartialDeep<IConfig> = {
  // server: {
  //   port: 3000,
  // },
};

export default configurations;
