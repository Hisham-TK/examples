import { DefaultConfig } from "./default";
// import { PartialDeep } from "../src/types/PartialDeep";
import { PartialDeep } from "../src/types/PartialDeep";

// const config: PartialDeep<DefaultConfig> = {
const config: PartialDeep<DefaultConfig> = {
  payment: {
    // merchantId: 1234,
  },
};
export default config;
